# Global environment configuration for systemd services

PYTHONUNBUFFERED=1
FLASK_ENV=production

DATA_DIR=/opt/ptp-data
SCENARIO_DIR=/opt/ptp-data/scenarios
REPORT_DIR=/opt/ptp-data/reports
EXPORT_DIR=/opt/ptp-data/exports
LOG_DIR=/var/log/ptp-dashboard
