# Example NAS mount configuration
# Edit these values on the Pi before enabling the mount

[NAS]
enabled = true
server = 192.168.1.50
share = /timing-archive
mount_point = /mnt/nas
type = nfs
options = defaults,noatime
